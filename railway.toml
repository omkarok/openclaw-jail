[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
# Railway injects $PORT; the entrypoint.sh script picks it up automatically
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
